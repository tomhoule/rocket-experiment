<!DOCTYPE html>
<html>
    <head>
        <meta charset="utf-8" />
        <title>Ethica ordine geometrico demonstrata</title>
        <link rel="stylesheet" href="/static/index.css" />
    </head>
    <body>

        <div class="hero is-primary">
            <div class="hero-body">
                <div class="container">
                    <h1 class="title">
                        {{#if title}}{{title}}{{else}}Locutions{{/if}}
                    </h1>
                    {{#if subtitle}}
                        <h2 class="subtitle">
                            {{subtitle}}
                        </h2>
                    {{/if}}
                    {{#if author}}
                        <h2 class="subtitle">{{author}}</h2>
                    {{/if}}
                </div>
            </div>
        </div>

